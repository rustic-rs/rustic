[backup.hooks]
run-before = ["echo 'Running backup hooks before' >> /tmp/backup_hooks.log"]
run-after = ["echo 'Running backup hooks after' >> /tmp/backup_hooks.log"]
run-failed = ["echo 'Running backup hooks failed' >> /tmp/backup_hooks.log"]
run-finally = ["echo 'Running backup hooks finally' >> /tmp/backup_hooks.log"]
