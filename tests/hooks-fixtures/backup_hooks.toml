[backup.hooks]
run-before = ["echo 'Running backup hooks before' >> backup_hooks.log"]
run-after = ["echo 'Running backup hooks after' >> backup_hooks.log"]
run-failed = ["echo 'Running backup hooks failed' >> backup_hooks.log"]
run-finally = ["echo 'Running backup hooks finally' >> backup_hooks.log"]
