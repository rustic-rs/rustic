[repository.hooks]
run-before = ["echo 'Running repository hooks before'"]
run-after = ["echo 'Running repository hooks after'"]
run-failed = ["echo 'Running repository hooks failed'"]
run-finally = ["echo 'Running repository hooks finally'"]
